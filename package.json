{
  "name": "seeded-selfsigned",
  "version": "0.1.0",
  "description": "Generate deterministic self-signed certificates with a seed from node.js",
  "main": "index.js",
  "scripts": {
    "test": "node test"
  },
  "repository": "laggingreflex/seeded-selfsigned",
  "dependencies": {
    "node-forge": "^0.10.0",
    "selfsigned": "^1.10.8"
  }
}
